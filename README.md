# RiemannBenchmark

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add `riemann_benchmark` to your list of dependencies in `mix.exs`:

    ```elixir
    def deps do
      [{:riemann_benchmark, "~> 0.1.0"}]
    end
    ```

  2. Ensure `riemann_benchmark` is started before your application:

    ```elixir
    def application do
      [applications: [:riemann_benchmark]]
    end
    ```

